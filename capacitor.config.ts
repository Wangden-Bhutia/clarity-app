import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stillmindlabs.clarity',
  appName: 'clarity-app',
  webDir: 'client/dist'
};

export default config;
